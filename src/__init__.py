"""
Package marker for the src package.

Creating this file ensures `src` is an importable package when running
modules with `python -m src.<module>` or when adjusting sys.path for
script execution. Keep it empty or add package-level imports if needed.
"""
